# messages Schema Reference

Owned by `app_venny_chat`.

Primary key: `message_id`

JSONB fields:

- `message_attributes`
- `message_attachments`
- `message_readby`

Each message belongs to a `thread_id`.
