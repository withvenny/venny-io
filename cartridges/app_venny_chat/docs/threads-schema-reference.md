# threads Schema Reference

Owned by `app_venny_chat`.

Primary key: `thread_id`

JSONB fields:

- `thread_attributes`
- `thread_participants`

Standard fields include `created_for_app_id`, `access`, `status`, `active`, `time_started`, and `time_updated`.
